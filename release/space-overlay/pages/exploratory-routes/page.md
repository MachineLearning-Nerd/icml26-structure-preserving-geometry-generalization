# Alternative routes for the four blocked claims

## Formal boundary

Claims 2, 3, 4, and 6 remain `BLOCKED`. No result below uses the paper's exact
trained Geo-NeW model, and all mechanism experiments are labeled `TOY`.

## Routes attempted

1. Exact-asset recovery audited the official tree and history, forks,
   releases, Actions artifacts, author Hugging Face namespace, earlier CNWF
   code, standard-data lineage, and LaMO comparator release.
2. Falsifiability contracts make Claims 2–4 accept/reject decisions executable
   and reject deliberately contradictory observations.
3. Four deterministic CPU analogues test whether a geometry-aware constrained
   solve can outperform a direct/template surrogate in small related systems.

All damaged-structure controls are detected. Claim 6 remains underdetermined
as written because “meaningful” has no numerical threshold; the toy threshold
is explicitly not attributed to the paper.
