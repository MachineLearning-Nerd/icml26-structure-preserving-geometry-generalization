# Toy mechanism EVAL

The machine-readable result is generated by the fixed cumulative command.
Every damaged-structure control must have greater mean normalized L2 error than
its structured analogue, and the independent checker exits nonzero otherwise.

Formal claim verdicts remain `BLOCKED`. These experiments are useful only as
downscaled mechanism evidence and as executable preparation for a future
full-scale run.

