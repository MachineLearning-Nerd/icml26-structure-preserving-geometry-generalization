[project]
name = "icml26-structure-preserving-geometry-generalization"
version = "0.1.0"
description = "Clean-room audit of Geo-NeW geometry generalization claims, ICML 2026, OpenReview RtnSbA5AUV."
requires-python = ">=3.11,<3.13"
dependencies = ["numpy", "scipy"]
[tool.pytest.ini_options]
pythonpath = ["repro/src"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
